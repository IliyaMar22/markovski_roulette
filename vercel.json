{
  "buildCommand": "cd frontend && npm install && npm run build && cd .. && mkdir -p dist && cp -r frontend/dist/* dist/",
  "outputDirectory": "dist",
  "installCommand": "npm install --prefix frontend"
}
